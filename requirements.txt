# Microworkers Alerts (Telegram Edition) — Python dependencies
# Install with: pip install -r requirements.txt

# HTTP client (used for both MW scraping AND Telegram API calls)
requests==2.31.0

# HTML parsing
beautifulsoup4==4.12.3
lxml==5.2.1

# Environment variables from .env file
python-dotenv==1.0.1
